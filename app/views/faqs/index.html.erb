<!--FAQ一覧の土台になるページ。参考にするのはstatic-pages/home.html-->

<% provide(:title, 'FAQ一覧') %>

<div class="row">
  <aside id="aside" class="col-md-4">
    <section id="question" class="question_info">
      <%= render 'shared/question_info' %>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @faqs.any? %>
      <h3>FAQ検索</h3>
      <%= form_tag(faqs_path,:method => 'get') do %>
        <%= text_field_tag :search, '', placeholder: "質問を入力してください..." %>
        <%= submit_tag '検索', :name => nil %>
      <% end %>
      
      <h3>FAQ一覧</h3>
      <ol id="faqs">
        <%= render @faqs %>
      </ol>
      
      <%= will_paginate %>
    <% end %>
  </div>
</div>

<%= javascript_pack_tag 'faq/faqIndex' %>