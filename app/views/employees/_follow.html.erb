<%= form_with(model: current_employee.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @employee.id %></div>
  <%= f.submit "フォロー", class: "btn btn-primary" %>
<% end %>